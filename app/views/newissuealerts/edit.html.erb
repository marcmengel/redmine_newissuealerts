<h2><%= l(:newissuealerts_edit, :project => @project.name) %></h2>
<!-- form_for should be here : -->
<%= form_tag( newissuealert_edit_path(@project.id,@newissuealert.id))  %>
<!-- <form action="<%= newissuealert_edit_path(@project.id) %>" method="post"> -->
 <%= l(:email_addresses) %> 
 <input type="text" name="mail_addresses" value="<%=@newissuealert.mail_addresses%>"> <br />
 <em><%= l(:email_addresses_comma_separated) %></em>
 <%= l(:enabled) %> 
 <input type="checkbox" name="enabled" checked="<%=@newissuealert.enabled%>"> <br />
 <%= l(:newissuealerts_set_priority) %>
 <input type="checkbox" name="priority" checked="<%=@newissuealert.priority%>"> <br />
 <input type="submit" value="submit">
</form> 

<!-- formshould be done this way, but comes up blank!.. -->

<% form_for @newissuealert do |f| %>
<div class="box tabular settings">
  <p>
    <%= f.label  l(:email_addresses) %> <%= f.text_field :mail_addresses, :size => 60 %><br />
    <em><%= l(:email_addresses_comma_separated) %></em>
  </p>
  <p>
   <%= f.label  l(:enabled) %> <%= f.check_box :enabled %> 
  </p>
  <p>
    <%= f.label  l(:newissuealerts_set_priority) %> <%= f.check_box :priority %>
  </p>
  <p>  <%= submit_tag l(:save), :name => "save" %> <%= submit_tag l(:delete), :name => "delete", :confirm => l(:newissuealerts_deletion_confirm) %> </p>
</div>
<% end %>

